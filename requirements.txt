sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface — add whichever you use for Milestone 5:
gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — offline PDF -> Markdown (layout-aware, scientific PDFs):
marker-pdf>=1.0.0
# docling>=2.0.0          # fallback converter if marker is slow/painful on this machine

# Stretch — hybrid search (BM25 + semantic):
# rank-bm25>=0.2.2